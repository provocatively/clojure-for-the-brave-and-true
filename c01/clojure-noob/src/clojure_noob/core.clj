; run this program in terminal with "lein run"

(ns clojure-noob.core                                       ; declare a namespace
  (:gen-class))


(defn -main                                                 ; entry point to program
  "I don't do a whole lot ... yet."
  [& args]
  (println "I'm a little teapot!"))
